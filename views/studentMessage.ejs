<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <!-- Font Awesome -->
<link
href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
rel="stylesheet"
/>
<!-- Google Fonts -->
<link
href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap"
rel="stylesheet"
/>
<!-- MDB -->
<link
href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.0/mdb.min.css"
rel="stylesheet"
/>
<script src="<%= socketIOClientScript %>"></script>
<style>
    body {
  background-color: #fbfbfb;
}
@media (min-width: 991.98px) {
  main {
    padding-left: 240px;
  }
}

/* Sidebar */
.sidebar {
  position: fixed;
  top: 0;
  bottom: 0;
  left: 0;
  padding: 58px 0 0; /* Height of navbar */
  box-shadow: 0 2px 5px 0 rgb(0 0 0 / 5%), 0 2px 10px 0 rgb(0 0 0 / 5%);
  width: 240px;
  z-index: 600;
}

@media (max-width: 991.98px) {
  .sidebar {
    width: 100%;
  }
}
.sidebar .active {
  border-radius: 5px;
  box-shadow: 0 2px 5px 0 rgb(0 0 0 / 16%), 0 2px 10px 0 rgb(0 0 0 / 12%);
}

.sidebar-sticky {
  position: relative;
  top: 0;
  height: calc(100vh - 48px);
  padding-top: 0.5rem;
  overflow-x: hidden;
  overflow-y: auto; /* Scrollable contents if viewport is shorter than content. */
}

.message {
    margin-bottom: 10px;
    padding: 10px;
    border-radius: 5px;
    font-size: 16px;

    
  }
  
  .sent {

    
    background-color: #FF9800;
    color: white;
    text-align: right;
    border-radius: 5px;
    padding: 20px;
    margin-left: 50%;
    margin-top: 5px;
    margin-bottom: 5px;
    list-style-type:none;
}
  
  
  .received {
    
    background-color: #2196F3;
    color: white;
    text-align: left;
    border-radius: 5px;
    padding: 20px;
   margin-right: 50%;
   margin-top: 5px;
    margin-bottom: 5px;
    list-style-type:none;
    
  }

</style>
    <title>Document</title>
</head>
  <body>
    
   
     
    <!--Main Navigation-->
<header>
  <!-- Sidebar -->
  <nav id="sidebarMenu" class="collapse d-lg-block sidebar collapse bg-white">
    <div class="position-sticky">
      <div class="list-group list-group-flush mx-3 mt-4">
        <a
          href="/student"
          class="list-group-item list-group-item-action py-2 ripple"
          aria-current="true"
        >
          <span>Main dashboard</span>
        </a>
        <a href="/student" class="list-group-item list-group-item-action py-2 ripple ">
          <i class=" fa fa-light fa-gauge me-3"></i></i><span>Dashboard</span>
        </a>
        <a href="/student/thesis" class="list-group-item list-group-item-action py-2 ripple"
          ><i class="fa fa-book fa-fw me-3"></i><span>Thesis</span></a
        >
        <a href="/student/myproject/<%- user.ID %>" class="list-group-item list-group-item-action py-2 ripple "
        ><i class="fa fa-duotone fa-book-open-reader me-3"></i></i><span>My Project</span></a
      >
        <a href="/student/message" class="list-group-item list-group-item-action py-2 ripple active"
          ><i class=" fa fa-brands fa-rocketchat me-3"></i><span>Messages</span></a
        >
        <a href="/plagiarism" class="list-group-item list-group-item-action py-2 ripple"
          ><i class=" fa fa-light fa-spell-check me-3"></i><span>Plagiarism</span></a
        >
        <!-- <a href="/studentReport" class="list-group-item list-group-item-action py-2 ripple"
          ><i class="fa fa-duotone fa-marker me-3"></i><span>Report</span></a
        > -->
        
        <a href="/logout" class="list-group-item list-group-item-action py-2 ripple">
          <i class="fa fa-duotone fa-right-from-bracket me-3"></i><span>LogOut</span>
        </a>
       
       
      </div>
    </div>
  </nav>
  <!-- Sidebar -->

  <!-- Navbar -->
  <nav id="main-navbar" class="navbar navbar-expand-lg navbar-light bg-white fixed-top">
    <!-- Container wrapper -->
    <div class="container-fluid">
      <!-- Toggle button -->
      <button
        class="navbar-toggler"
        type="button"
        data-mdb-toggle="collapse"
        data-mdb-target="#sidebarMenu"
        aria-controls="sidebarMenu"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <i class="fas fa-bars"></i>
      </button>

      <!-- Brand -->
      <a class="navbar-brand" href="#">
        <!-- <img
          src="https://mdbcdn.b-cdn.net/img/logo/mdb-transaprent-noshadows.webp"
          height="25"
          alt="MDB Logo"
          loading="lazy"
        /> -->
      </a>
     
       <!-- Right links -->
       <ul class="navbar-nav ms-auto d-flex flex-row">
       
       
        <li class="nav-item dropdown  me-5">
          welcome, 
          <a class=" dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
            <%- user.fName%>
          </a>
          <ul class="dropdown-menu dropdown-menu-dark">
            <li><a class="dropdown-item" href="#">Profile</a></li>
            <li><a class="dropdown-item" href="#">Settings</a></li>
            <li>
            <form action="/logout" method="post">
             <a class="dropdown-item" href="#">Logout</a>
            </form>
          </li>
          
          </ul>
        </li>
        
        <div class="btn-group">
          <button type="button" class="btn btn-danger " >
            Logout
          </button>
        </div>
        
        


      </ul>
    </div>
    <!-- Container wrapper -->
      
  </nav>
  <!-- Navbar -->
</header>
<!--Main Navigation-->












  <body>
    











    <main style="margin-top: 58px;">
      <div class="container pt-4  ms-5 me-5 text-center">
                  
                    <div class="card p-3  " id="messages" style="height: 500px;overflow-y: scroll; margin-right:90px;">
                      <% for (const chat of chats) { %>
                        <li class="<%= chat.sender === 'student' ? 'sent' : 'received' %>">
                          <% if (chat.sender === 'student') { %>
                            You: <%= chat.message %>
                          <% } else { %>
                            <%= chat.sender %>: <%= chat.message %>
                          <% } %>
                        </li>
                      <% } %>
               <!-- Chat UI -->


                    </div>
                    <div class=""style='margin-right:90px'>
                    <form id="message-form" action="/student/message" method="post">
                      <div class="input-group " style="margin-top: 10px;">
                      <input type="text" name="message" class="form-control" id="message-input" autocomplete="off" >
                      <!-- <button class="btn btn-outline-secondary" >send</button> -->
                      <button class="btn btn-success input-group-text"  style="color: black;">send</button>
                    </form>
                  </div>
                    <!-- Socket.io logic -->
                    <script>
                      // Connect to the Socket.io server
                      const socket = io();
                    
                      // Join the student's room with their supervisor
                      const supervisorRoom = '<%= room %>';
                      socket.emit('join room', { room: supervisorRoom });
                    
                      // Handle received chat messages
                      socket.on('chat message', (message) => {
                        const { sender, message: msg } = message;
                        const messageElement = document.createElement('li');
                    
                         // Assign class based on the sender
                      if (sender === 'student') {
                        messageElement.classList.add('sent');
                      } else {
                        messageElement.classList.add('received');
                      }
                        
                        messageElement.textContent = `${sender}: ${msg}`;
                        document.getElementById('messages').appendChild(messageElement);
                      });
                    
                      // Handle form submission
                      document.getElementById('message-form').addEventListener('submit', (event) => {
                        event.preventDefault();
                        const input = document.getElementById('message-input');
                        const message = input.value.trim();
                        if (message) {
                          // Emit the chat message to the supervisor's room
                          const studentid= "<%=studentId%>"

                          socket.emit('chat message', { sender: 'student', room: supervisorRoom, message , studentid});
                          
                                // Display the sent message in the chat UI
                                const messageElement = document.createElement('li');
                                messageElement.classList.add('sent');
                          messageElement.textContent = `You: ${message}`;
                          document.getElementById('messages').appendChild(messageElement);
                    
                          
                          input.value = '';
                        }
                      });
                    </script>
                </div>
            </div>

            </div>
            
        </div>
      </div>

      <script
  type="text/javascript"
  src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.0/mdb.min.js"></script>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
  </body>
</html>